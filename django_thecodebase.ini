[uwsgi]
project = django_thecodebase
base = /opt/django_thecodebase

chdir = %(base)/%(project)
home = %(base)/%(project)
module = %(project).wsgi:application

master = true
processes = 5

socket = %(base)/%(project)/%(project).sock
chmod-socket = 664
vacuum = true